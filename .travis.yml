sudo: required

services:
    - docker 


before_install:
      - docker build -t docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true  docker-react npm run test

deploy:
   provider: elasticbeanstalk
   region: "ap-south-1"
   app: "Docker"
   env: "Docker-env"
   bucket_name: "elasticbeanstalk-ap-south-1-671141137976"
   bucket_path: "Docker"
   on:
     branch : master

   access_key-id: "AKIAZYQZBBY4PVVOFCTK"
   secret_access_key:
         secure: "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"

